Interface
*Enhance the scenario editor with ajax requests
*Add a function to ajust a color already assigned
*Modify jscolor to display color when typing values
*Add a visual patcher for fixtures (like the one for ola rdm)

Base
*Make an index page to share fixture profiles
*Make a program to import XML fixture files from QLC

Code
*Declare all variables to allow register_globals = Off
*Use SQL classes which allow the database system choice
